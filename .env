
# -----------------------------------------------------------------------------
# 🗄️  Настройки базы данных
# -----------------------------------------------------------------------------

# 1) Для локальной разработки без Docker (SQLite, один файл в корне):
# REPA_DB_URL=sqlite+aiosqlite:///./repa.sqlite3

# 2) Для запуска через docker-compose.dev.yml (PostgreSQL внутри контейнера):
REPA_DB_URL=postgresql+asyncpg://repa_user:repa_pass@db:5432/repa

# Автосоздание таблиц при старте (только в dev!)
REPA_AUTO_CREATE_TABLES=True

# Логировать SQL-запросы в консоль (True = больше логов)
REPA_ECHO_SQL=False

# Размер пула соединений (актуально для PostgreSQL)
REPA_POOL_SIZE=5


# -----------------------------------------------------------------------------
# 🔐  JWT / Авторизация
# -----------------------------------------------------------------------------

# Секретный ключ для подписи JWT (замени в продакшене!)
REPA_JWT_SECRET=change-this-to-long-random-string

# Идентификаторы токена
REPA_JWT_ISSUER=repa
REPA_JWT_AUDIENCE=repa-users

# Время жизни access-токена в минутах (720 = 12 часов)
REPA_JWT_EXP_MINUTES=720


# -----------------------------------------------------------------------------
# 💰  Бизнес-настройки
# -----------------------------------------------------------------------------

# Валюта (отображается в API при тарифах и оплате)
REPA_CURRENCY=RUB

# Время жизни демо-сессии (в днях)
REPA_DEMO_TTL_DAYS=7

# Процент скидки по умолчанию для выданных промокодов
REPA_PROMO_DEFAULT_PERCENT=10


# -----------------------------------------------------------------------------
# 🌍  CORS / разрешённые источники фронта
# -----------------------------------------------------------------------------

# Список доменов через запятую, с которых разрешены запросы к API
# Пример: http://localhost:5173,https://repa.dev,https://repa.app
REPA_CORS_ALLOW_ORIGINS=http://localhost:5173


# -----------------------------------------------------------------------------
# ⚙️  Дополнительные параметры
# -----------------------------------------------------------------------------

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Название и версия приложения (отображаются в Swagger)
REPA_APP_NAME=REPA-MVP
REPA_APP_VERSION=0.2.0
